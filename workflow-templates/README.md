# Workflow Templates — Alyp.Studio

Templates reutilizables de GitHub Actions para todos los proyectos de clientes.

## Contenido
- `pr-checks.yml` — Lint, TypeScript, build check (se agrega en Fase 2)
- `staging-deploy.yml` — Deploy automático a staging (se agrega en Fase 3)
- `prod-deploy.yml` — Deploy controlado a producción (se agrega en Fase 3)
